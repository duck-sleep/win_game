# 10a — 频率主线工单（给执行模型，一次只做一号）

> 方案背景在 `10_动态触觉反馈技术.md`「下一步」章。本文件才是动手清单。
> **用户开新对话时只贴下面「开场白」+ 当前工单号。不要让模型一次做完。**

---

## 开场白（复制到新对话）

```
读 D:\win_game_project\12_win_上机跑\10a_频率工单_给执行模型.md
只做工单 <填号，例如 W1.1>。
做完：在该工单打 [x]、写下「实际产物路径」、停下。不要做下一号。
遵守文内「全局禁令」。不要 git commit，除非我当场说提交。
```

当前进度：全部未做。建议按号执行；W2 可在 W1.2 完成后与 W1.3 并行。
**W3A（官方参数化接口）必须先于 W3B（USB 逆向）。**

---

## 全局禁令（每张工单都有效）

1. 不碰安卓 / VM / 刷机 / `ff_bridge` / A2DP / `11_安卓雷云技术.md`。
2. 不 `git add -A`。不改 `hapticx-android`。不改 BSP。
3. **禁止**跑 `D:\win_game_project\13_win_cap\captures\pcap_setup.ps1`（里面的控制器热重绑会弄丢 USBPcap 设备）。
4. 不改 `hapticx\xinput_out.py` 的振动语义（W4 之前它只负责 ERM 转速）。
5. **不要改变** `A2HAnalyzer.process_block` 的返回值个数。现在是 4 元组 `(left, right, band, beat)`。新字段写到 **实例属性**，见 W2.1。
6. 听诊 / 抓包 / HID 自检时，雷云 SENSA 和 HapticX **只开一边**。
7. 手柄抓 SENSA 时保持 VID `1532` PID `0a3f`。不要切到 `0a4c` PC 模式。
8. 本阶段不实现 `amp_comp`、不安 Switch 马达、不接 Steam/DualSense（除非工单 W4.6 被用户点名）。
9. Python 用完整 3.13，不是 TRAE 精简版：
   `"%LOCALAPPDATA%\Programs\Python\Python313\python.exe"`
10. 工作目录默认：`D:\win_game_project\25_xbox_control\hapticx`
11. pcap / 脚本路径只用英文：`D:\win_game_project\13_win_cap\captures\`
12. 做完就停。不要「顺便重构」。
13. 不得把“高级触觉”擅自写成“已证实 LRA”；官方未公开 Wolverine 执行器类型。
14. 不得假设 USB 包一定有显式 Hz 字节；也可能是 normalized pitch、频带强度、索引或编码波形。
15. 不得假设 `hidapi` 一定能写 0a3f；先确认目标接口是否真是 HID collection、是否被驱动独占。

---

## 环境速查

| 项 | 值 |
|----|-----|
| Win 工程 | `D:\win_game_project\25_xbox_control\hapticx` |
| 启动 GUI | 双击 `D:\win_game_project\25_xbox_control\启动HapticX.bat` |
| 探针 | `python razer_probe.py`（先 `cd` 到 hapticx） |
| 测试音输出目录 | `D:\win_game_project\25_xbox_control\hapticx\tones\` |
| 听诊记录 | `D:\win_game_project\12_win_上机跑\captures_freq\听诊记录.md` |
| pcap 目录 | `D:\win_game_project\13_win_cap\captures\` |
| USBPcapCMD | `C:\Program Files\USBPcap\USBPcapCMD.exe` |
| tshark | `C:\Program Files\Wireshark\tshark.exe` |
| 手柄 | Razer Wolverine，USB `VID_1532&PID_0A3F` |
| 雷云 | Razer App Engine / Synapse；SENSA = 音效转触觉开关 |

---

## 总路线（不要跳）

```
W0 日志/API能力 → W1 A2H 人手听诊 → W2 软件吐主频+pitch
                                    → W3A 官方参数化直控（优先）
W3A 不通才走 W3B 全量 USB 建模 → W4 设备适配 → W5 接回 GUI
```

W4 必须满足其一：W3A 固定 amplitude、三档 pitch 直控成功；或 W3B 的协议模型通过留出样本验证。

## 最终结论边界（执行模型不得夸大）

本工单能提高“做成”的概率，但**不能预先保证这只 Wolverine 一定能被第三方程序直控 pitch**。
若官方运行时拒绝第三方、设备协议受签名/驱动独占保护、且逆向模型不能复现，
正确结论是“这只手柄路线受阻，换公开接口执行器”，不是继续调 XInput。

---

# W0 日志里马达会什么

### W0.1 [ ] 用户：让雷云日志是热的

- 谁：用户
- 做：插手柄、开雷云、等 30 秒看到设备。不要开 HapticX。
- 过关：任务管理器里有 `RazerAppEngine` 或 `RazerSynapse`。
- 停。

### W0.2 [ ] AI：跑探针，存原始 JSON

- 谁：AI（本机 Shell）
- 依赖：W0.1
- 命令（PowerShell）：

```
$py = "$env:LOCALAPPDATA\Programs\Python\Python313\python.exe"
cd D:\win_game_project\25_xbox_control\hapticx
& $py razer_probe.py > D:\win_game_project\12_win_上机跑\captures_freq\razer_probe_raw.txt
```

若目录不存在就建 `D:\win_game_project\12_win_上机跑\captures_freq`。

- 过关：`razer_probe_raw.txt` 里同时有 `========== 雷云4 侦察结果 ==========` 和 `========== 原始JSON ==========`；JSON 里能看到 `a2h`。
- 失败：`A2H 配方 : 未找到` → 停，让用户再开雷云放 10 秒音乐后重跑，不要改 `razer_probe.py`。
- 停。不要在本工单改 Python。

### W0.3 [ ] AI：从 JSON 写一页能力说明书

- 谁：AI（只写 md，不改产品代码）
- 依赖：W0.2
- 新建：`D:\win_game_project\12_win_上机跑\captures_freq\雷云马达能力.md`
- 必须从 JSON 抄出这些字段（没有就写「日志无此键」）：

  - `encoderDLL`
  - `profile.compressor.amp_comp` / `gamma`
  - `profile.underlying_bands`：几条 band；每条 `frequency_range`、`amplitude_window_length`、`frequency_window_length`、`minimal_amplitude_threshold`
  - `deviceReport.NumberBodyPart`
  - 每个 Bodypart 的 `FrequencyMin` / `FrequencyMax`
  - `mixerGains`

- 文末加三句人话：几路马达、Hz 范围、编码器 DLL 文件名。
- 过关：md 存在且数字来自 JSON，不是猜的。
- 停。

### W0.4 [ ] 用户：确认还是 0a3f

- 谁：用户
- 看设备管理器或探针输出：VID 1532、PID **0a3f**。
- 若是 0a4c：停整条频率线，先切回雷云/预设模式再继续。
- 停。

### W0.5 [ ] AI：建立“公开接口可用性”证据页

- 谁：AI（只查官方资料和本机日志，不下载、不安装）
- 依赖：W0.3
- 新建：`D:\win_game_project\12_win_上机跑\captures_freq\公开接口调查.md`
- 必须核实并附来源 URL：
  - Interhaptics/Wyvrn `.haps` 是否包含 amplitude 与 pitch/frequency
  - `AddParametricEffect` 的 amplitude、pitch、freqMin、freqMax、transient 参数
  - 当前 SDK/Haptic Composer 是否列出 Razer Sensa 设备测试支持
  - 授权/分发条件是否允许本项目调用（查不到就写“待人工确认”，禁止猜）
- 必须写入一个换算式：
  `pitch_norm = clamp((f_hz - freqMin) / (freqMax - freqMin), 0, 1)`
- 过关：结论分成“官方明确”“本机日志明确”“仍未知”三段。
- 停。不要立刻写 USB 协议。

### W0.6 [ ] AI：只读检查本机编码器/运行时

- 谁：AI
- 依赖：W0.2
- 从 `razer_probe_raw.txt` 的 `encoderDLLPath` 取**真实路径**，不要猜 DLL 名。
- 只读记录：文件是否存在、版本信息、数字签名、同目录相关 DLL。
- 可用 PowerShell `Get-Item ... | Select VersionInfo`；若 VS 工具存在可用 `dumpbin /exports`，不存在就写“未检查 exports”，不要安装工具。
- 追加到 `公开接口调查.md`。
- 禁止：注入雷云进程、修改/复制系统 DLL、绕过签名。
- 停。

---

# W1 黑盒听诊（先手感，不抓包）

### W1.1 [ ] AI：写正弦 / 小星星生成脚本

- 谁：AI
- 依赖：无（可与 W0 并行）
- 新建 **一个文件**：`D:\win_game_project\25_xbox_control\hapticx\tools\gen_freq_tones.py`
- 要求（照做，不要加 GUI）：

  - 采样率 48000，单声道，16-bit PCM wav
  - 输出目录 `D:\win_game_project\25_xbox_control\hapticx\tones\`（脚本自己建）
  - 生成：
    - `tone_050.wav` 50Hz 8 秒
    - `tone_080.wav` 80Hz 8 秒
    - `tone_110.wav` 110Hz 8 秒
    - `tone_130.wav` 130Hz 8 秒（A2H 上边界）
    - `tone_200.wav` 200Hz 8 秒（A2H 带外负对照，不代表输出 200Hz）
    - `star.wav`：真正的小星星首句 C2 C2 G2 G2 A2 A2 G2：
      `65.41,65.41,98.00,98.00,110.00,110.00,98.00Hz`；
      每音 0.6s，音间 0.1s 静音
  - **全部 wav 的 RMS 相同**（先生成正弦再除以 RMS 再乘 0.20）
  - 每段正弦首尾加 15ms 淡入/淡出，避免点击声被瞬态检测当成触觉
  - 脚本打印每个文件的路径、时长、RMS
  - 只用标准库 + 已有 numpy。不要新依赖。

- 过关：文件存在，`python tools\gen_freq_tones.py --help` 或直接跑无报错。
- 停。不要在本工单改 analyzer。

### W1.2 [ ] AI：跑脚本并核对 RMS

- 谁：AI
- 依赖：W1.1
- 命令：

```
$py = "$env:LOCALAPPDATA\Programs\Python\Python313\python.exe"
cd D:\win_game_project\25_xbox_control\hapticx
& $py tools\gen_freq_tones.py
```

- 过关：六个 wav 都在 `tones\`；打印的 RMS 彼此相对误差 < 5%。
- 把打印原文追加到 `captures_freq\听诊记录.md` 顶部。
- 停。

### W1.3 [ ] 用户：L1 只开雷云听四条正弦

- 谁：用户（AI 只准备记录模板，不代替听）
- 依赖：W1.2、W0.4
- 准备：HapticX **退出**。雷云 SENSA **开**。系统音量固定，中等。
- 依次放 `tone_050` → `080` → `110` → `130` → `200`。每条听完填表：

```
## L1 雷云 SENSA + 正弦
音量滑条位置: ___
| 文件 | 手上音高（低沉/中/尖细/分不出） | 强弱 | 备注 |
| tone_050 |  |  |  |
| tone_080 |  |  |  |
| tone_110 |  |  |  |
| tone_130 |  |  |  |
| tone_200 |  |  |  |
50/80/110（带内）能分出不同手感?  是 / 否
200（带外）是否明显变弱或静音?  是 / 否
```

- 过关：表填完。
- 停。AI 不要在本工单分析结论（那是 W1.6）。

### W1.4 [ ] 用户：L2 雷云听 star.wav

- 谁：用户
- 依赖：W1.3
- 仍只开 SENSA。放 `star.wav` 两遍。
- 记录：

```
## L2 雷云 + star.wav
能听出音阶（不只是一串轻重）?  是 / 否 / 不确定
```

- 停。

### W1.5 [ ] 用户：H1/H2 只开 HapticX

- 谁：用户
- 依赖：W1.2
- 雷云里 **关掉 SENSA**（进程可留）。双击 `启动HapticX.bat`，档位 **均衡**，开捕获。
- 再放同一组 tone + star。记录：

```
## H1 HapticX 均衡 + 正弦
| 文件 | 有音高差? | 只有强弱? |
| tone_050 |  |  |
| tone_200 |  |  |
## H2 HapticX + star.wav
听起来像音阶?  是 / 否 / 不确定
注意：即使“是”，也可能只是 ERM 转速随强度变化，不能证明同振幅下独立控 pitch。
```

- 过关：记录完整即可。真正验收是 W4.4 固定 amp 换 pitch、再固定 pitch 换 amp。
- 停。

### W1.6 [ ] AI：根据听诊选分支（只写结论，不动代码）

- 谁：AI
- 依赖：W1.3–W1.5 记录已填
- 在 `听诊记录.md` 文末写入 **只能三选一**：

  - **分支 A**：50/80/110 手感不同；只能说明 A2H 输出随输入变化，**不能单靠手摸证明输出 Hz**
  - **分支 B**：带内音手感接近，但雷云对音乐仍细；后续靠 W3A 固定 amplitude/pitch 直控判定
  - **分支 C**：雷云对正弦完全不震 → 先查 SENSA 开关/手柄模式，禁止进 W3

- 过关：文末有「分支 X」三个字。
- 200Hz 只用于验证 30–130Hz 输入筛选；禁止用它得出“马达输出 200Hz”。
- 停。把分支告诉用户。

---

# W2 软件吐音频特征 + A/pitch（手柄这时仍不会唱歌）

原则：`process_block` **仍返回 4 元组**。频率写实例属性。
`last_freq_hz` 是**音频主频估计**；`last_pitch_norm` 才是候选输出控制量。
两者不能在 UI/文档中写成同一个东西。

### W2.1 [ ] AI：analyzer 算出 freq_hz

- 谁：AI
- 依赖：W0.3（必须先拿到日志里的真实 freqMin/freqMax）+ W1.2
- 只改：`D:\win_game_project\25_xbox_control\hapticx\analyzer.py`
- 做：

  1. **不要复用现有 2400 点/50ms FFT 来估 pitch。** 48k/2400=20Hz/格，
     无法稳定区分小星星的 65/98/110Hz。另建约 125ms 的估频缓冲：
     `FREQ_EST_WINDOW = 6000`（8Hz/格），20ms hop；加 Hann 窗。
  2. `__init__` 增加：
     - `self.freq_buffer = np.zeros(FREQ_EST_WINDOW, ...)`
     - `self.last_freq_hz = 0.0`
     - `self.last_amp = 0.0`
     - `self.last_centroid_hz = 0.0`
     - `self.last_pitch_norm = 0.0`
     - `self.last_pitch_confidence = 0.0`
     - `self.last_band_energies = [0.0, 0.0, 0.0]`
  3. 每块样本同时推入 `freq_buffer`；在 30–130Hz 上估频：
     - 取 6000 点频谱峰，并用峰值左右相邻格做抛物线插值，得到 `last_freq_hz`
     - 计算频谱质心 `last_centroid_hz`
     - `last_pitch_confidence = 峰附近能量 / 30–130Hz 总能量`；静音或置信度不足时
       freq/pitch 归零，不沿用上次值
     - 按 **当前** `self.band_edges` 三段 RMS → `last_band_energies`（长度 3）
  4. `process_block` 末尾：`self.last_amp = float(compressed * self.gain)`（或与 `band_level` 一致的标量；在文件头注释写清用的是哪一个）
  5. 用 W0.3 的真实范围计算：
     `last_pitch_norm = clip((last_freq_hz - freqMin)/(freqMax-freqMin), 0, 1)`。
     把 freqMin/freqMax 定义成有来源注释的常量；若 W0.3 没拿到范围，停，不准猜 30–400。
  6. 对输出 pitch 加轻量中值/指数平滑，但纯音切换后 150ms 内要进入新值；低置信度时 A 可继续，
     pitch 不发送/归中性值。阈值要写常量并由 W2.2 测试。
  7. **返回值一行不准改**：仍是 `return float(left), float(right), float(smoothed), self.transient_amp > 0`

- 过关：`test_analyzer.py` 仍能跑（返回值没破）。
- 禁止：改 EQ、改 gamma、改左右马达公式。
- 停。

### W2.2 [ ] AI：离线测 50Hz / 110Hz 主频

- 谁：AI
- 依赖：W2.1
- 新建：`D:\win_game_project\25_xbox_control\hapticx\tools\test_freq_hz.py`
- 做：合成 1 秒 50Hz 正弦、1 秒 110Hz 正弦（48kHz，幅度 0.3），按 960 点块喂 `A2HAnalyzer(mode="balanced")`，丢掉前 10 块，对其余块的 `last_freq_hz` 取中位数。
- 测 50、65.41、80、98、110、130Hz；打印 median_freq / median_pitch / confidence。
- 过关：每个纯音估计误差 ≤5Hz；各音结果严格递增；65/98/110 可分；切音后 ≤150ms 收敛。
- pitch 必须都在 0–1，且 110 的 pitch > 50 的 pitch。
- 再喂静音与白噪声：静音 freq=0；低置信度噪声不得随机跳成“明确音符”。
- 命令：

```
$py = "$env:LOCALAPPDATA\Programs\Python\Python313\python.exe"
cd D:\win_game_project\25_xbox_control\hapticx
& $py tools\test_freq_hz.py
& $py test_analyzer.py
```

- 停。

### W2.3 [ ] AI：pipeline 把 F 放进 state

- 谁：AI
- 依赖：W2.1
- 只改：`hapticx\pipeline.py`
- 做：

  - `self.state` 增加键：`"audio_freq_hz": 0.0`, `"centroid_hz": 0.0`,
    `"pitch_norm": 0.0`, `"pitch_confidence": 0.0`, `"amp": 0.0`（不要删 left/right）
  - `_on_block` 在 `process_block` 之后：
    `self.state["audio_freq_hz"] = analyzer.last_freq_hz`，同理抄 centroid / pitch / amp
  - `stop_capture` 时把这五个键归零

- 禁止：根据 freq 去改 `set_vibration` 的参数。仍 `set_vibration(left, right)`。
- 停。

### W2.4 [ ] AI：命令行打印 F

- 谁：AI
- 依赖：W2.3
- 只改：`hapticx\main.py` 里 verbose 那一行（约 `BEAT` 那个 print）
- 做成：`L=.. R=.. AudioF=..Hz Pitch=.. Conf=.. A=..`
- `process_block` 仍解包 4 个；三个新值从 analyzer 实例属性读。
- 停。不要改 argparse 默认行为。

### W2.5 [ ] AI：GUI 显示 F 和 A

- 谁：AI
- 依赖：W2.3
- 改：`hapticx\app.py`（仪表板刷新处，现有 `vals` / 马达条附近）
- 做：增加一行只读文字，格式固定：
  `音频主频={audio_freq_hz:.0f} Hz    候选Pitch={pitch_norm:.2f}    Conf={pitch_confidence:.2f}    A={amp:.2f}`
- 用 `state.get(..., 0)`，避免 KeyError。
- 禁止：改四档逻辑、改卡片布局大翻修。
- 停。

### W2.6 [ ] 用户：放 tone，看屏幕 F 能分开

- 谁：用户
- 依赖：W2.5、W1.2
- SENSA 关，开 HapticX，放 `tone_050.wav` 看 F，再放 `tone_110.wav` 看 F。
- 过关：两个音频主频能分开（约 50 vs 110，允许 ±10Hz），pitch 随之单调变化且在 0–1。**手柄没有音阶是正常的。**
- 记到 `听诊记录.md`：

```
## W2.6 软件 F
tone_050 屏幕 AudioF / Pitch ≈
tone_110 屏幕 AudioF / Pitch ≈
能分开?  是 / 否
```

- 停。这就是同事说的「至少知道怎么控制频率」的软件半边。

### W2.7 [ ] 冻结（不是代码工单）

- 谁：任何人看到这条都要遵守
- W2 完成后再进入 W3 代码/抓包。下面这些 **直到用户点名** 都不做：`amp_comp`、改 `xinput_out.py`、改安卓 Kotlin。

---

# W3A 官方 amplitude + pitch 路线（永远先做）

### W3A.0 [ ] AI：给出可执行性判定，不安装

- 谁：AI
- 依赖：W0.5、W0.6
- 在 `公开接口调查.md` 文末只能写一个判定：
  - `A-可试`：官方 SDK/Composer 明确支持 Razer Sensa 测试，且有可获取的安装入口
  - `B-需授权`：接口存在，但许可、账号或下载需要用户/公司确认
  - `C-不可用`：当前公开运行时明确不支持该设备/第三方
  - `D-证据不足`：不能判断
- 停。A 才进 W3A.1；B/D 问用户；C 直接进 W3B.0。

### W3A.1 [ ] 用户：批准并安装官方工具/SDK

- 谁：用户（AI 不得自行接受许可协议或注册账号）
- 依赖：W3A.0=A，或用户已解决 B
- 安装来源必须是 W0.5 记录的官方 URL。
- 记录工具名、版本、安装目录到 `公开接口调查.md`。
- 停。

### W3A.2 [ ] AI：做“固定振幅、三档 pitch”最小样例

- 谁：AI
- 依赖：W3A.1
- 优先使用官方最小接口：`.haps`/Haptic Composer 测试，或公开
  `AddParametricEffect(amplitude, pitch, freqMin, freqMax, transient...)`。
- 固定 amplitude=0.5、固定时长 2s、固定 freqMin/freqMax（用 W0.3 真值）；
  只改变 pitch=0.1、0.5、0.9。三个效果之间留 1s 静音。
- 产物放：`D:\win_game_project\25_xbox_control\hapticx\official_pitch_probe\`
- 必须记录：
  - 调用的是哪个公开 API/工具
  - 参数原值
  - 目标设备是否被运行时枚举
  - 控制台/工具错误原文
- 禁止：把音频 tone 当成这一步；这一步必须直接改变触觉 pitch 参数。
- 停。

### W3A.3 [ ] 用户：三档 pitch 盲测

- 谁：用户
- SENSA/HapticX 普通 A2H 关掉，只运行 W3A.2 的官方样例。
- 随机顺序播放 0.1/0.5/0.9，每档至少重复 3 次，记录“低/中/高”判断。
- 过关：9 次中至少 7 次顺序判断正确，且强弱大致相当。
- 失败：若只有强弱变化，记录失败，不能宣布会控 pitch。
- 停。

### W3A.4 [ ] AI：公开路线闸门

- W3A.3 通过：在状态栏写 `公开 pitch 直控：通过`；**跳过 W3B**，W4 写官方运行时适配器。
- 运行时明确拒绝/设备不支持：写 `公开 pitch 直控：不可用 + 错误原文`；进入 W3B。
- 结果模糊：先检查参数/路由一次；仍模糊则进入 W3B，不得硬判成功。
- 停。

---

# W3B USBPcap 逆向（只在 W3A 不通时）

没有 W3A.4 的“不通/不可用”结论，不要开抓。W1 分支 C 也禁止抓。

### W3B.0 [ ] 用户：重启电脑，确认 USBPcap 设备在

- 谁：用户
- 做：重启。不要跑 `pcap_setup.ps1`。尽量别开 VMware USB 穿透。
- 管理员 PowerShell：

```
[System.IO.Directory]::GetFiles('\\.\','') | Where-Object { $_ -match 'USBPcap' }
```

- 过关：能看到 `\\.\USBPcap1`（或 2/3）。没有 → 再重启一次；仍没有 → 停，不要热重绑。
- 停。

### W3B.1 [ ] 用户：抓包前环境清单

- 谁：用户，逐项勾：

```
[ ] HapticX 已退出
[ ] 雷云 SENSA 已打开（W3B.4 及 STAR/SILENT）；XINPUT 对照时再关 SENSA
[ ] 手柄是 0a3f
[ ] 系统音量与 W1 听诊时相同
[ ] 虚拟机不要占用该 USB 手柄
```

- 停。

### W3B.2 [ ] AI：生成 3×3 因子实验音

- 谁：AI
- 依赖：W1.1
- 扩展 `gen_freq_tones.py`，另生成 9 个文件：
  - 频率：50、80、110Hz
  - RMS：0.10、0.20、0.30
  - 命名：`factor_F050_A10.wav` … `factor_F110_A30.wav`
- 每个 4 秒、首尾 15ms 淡入淡出。打印实测 RMS。
- 过关：每个同 A 档的 RMS 误差 <5%；频率和响度是两个独立变量。
- 停。

### W3B.3 [ ] 用户：锁定 Root Hub 并记录枚举地址

- Wireshark/USBPcapCMD 选手柄所在 Root Hub，**开始时先完整抓一次拔插/重连**。
- 不要设置只保留 VID/PID 的显示过滤；后续数据 URB 常不携带 VID/PID。
- 从枚举描述符找到 `idVendor=0x1532,idProduct=0x0a3f`，记录：

```
bus_id=___
device_address=___
```

- 写到 `D:\win_game_project\13_win_cap\captures\device_address.txt`。
- 停。

### W3B.4 [ ] 用户：抓 9 个因子场景

- 只开 SENSA，HapticX 退出，Windows 系统音量固定且全程不动。
- 每个文件单独抓，保存**完整 Root Hub 原始包**：
  `F050_A10_sensa.pcapng` ... `F110_A30_sensa.pcapng`。
- 不要在保存时只导出 `usb.idVendor` 显示包。
- 一次只抓一个；9 个文件齐后停。

### W3B.5 [ ] 用户：抓三个对照

- `STAR_sensa.pcapng`：只开 SENSA，播放 star。
- `SILENT_sensa.pcapng`：SENSA 开，静音 10 秒。
- `XINPUT_hapticx.pcapng`：关 SENSA，开 HapticX，播放 `factor_F050_A20.wav`。
- 三个都保存完整 Root Hub 原始包。
- 停。

### W3B.6 [ ] AI：用 tshark 按地址导出全部 OUT

- 谁：AI
- 依赖：W3B.3–W3B.5
- 不要自己写 pcap 解析器（易错）。用 tshark。
- 从 `device_address.txt` 读 bus/address；对 12 个 pcapng 都跑：

```
$ts = "C:\Program Files\Wireshark\tshark.exe"
$out = "D:\win_game_project\13_win_cap\captures"
& $ts -r "$out\F050_A20_sensa.pcapng" `
  -Y "usb.bus_id == <bus> && usb.device_address == <addr> && usb.endpoint_address.direction == 0" `
  -T fields -e frame.number -e frame.time_relative -e usb.transfer_type `
  -e usb.endpoint_address -e usb.capdata > "$out\F050_A20_sensa_out.txt"
```

固定事实：`endpoint_address.direction == 0` 是 OUT，`1` 是 IN。
USBPcap/Wireshark 中 `usb.transfer_type == 1` 是 Interrupt，`2` 是 Control。
本工单**不要限制 transfer_type**，control/bulk/interrupt 全留。

- 若导出为空，先检查同一 pcap 里的 device_address 是否因重新枚举改变；禁止把 direction 改成 1 冒充 OUT。
- 过关：每个场景 txt 非空，包含 transfer_type、endpoint、hex。
- 产物放同目录。不要在本工单猜协议。
- 停。

### W3B.7 [ ] AI：3×3 因子分析，建立候选模型

- 谁：AI
- 依赖：W3B.6
- 新建：`D:\win_game_project\13_win_cap\captures\协议候选模型.md`
- 做：

  1. 丢掉 SILENT 里也出现的周期性短包（当心跳）
  2. 在剩余包里找「长度相同、间隔稳定」的那类
  3. 找“随 50/80/110 单调变化且不受 A10/A20/A30 改变”的 pitch 候选
  4. 找“随 A10/A20/A30 单调变化且不受频率改变”的 amplitude 候选
  5. 看 XINPUT txt：是否能对上已知 GIP rumble 形（常见以 `09` 开头的 rumble；**不要**把它写成雷云 LRA 协议）

- 不准只盯单字节：同时考虑 normalized pitch、16/32-bit 端序、频带强度、包率、分块/波形。
- 若没有可分离候选：写“未建立模型”，不要编造字段，停止 W3B。
- 过关：候选项都有“支持证据”和“反证/未知”。
- 停。

### W3B.8 [ ] 用户+AI：留出样本交叉验证

- 依赖：W3B.7 有候选模型
- AI 先生成模型**未见过**的 `factor_F095_A25.wav`（95Hz，RMS 0.25，4秒）。
- 用户只开 SENSA 抓 `F095_A25_holdout.pcapng`。
- AI 用候选模型在看答案前预测相关字段/包率，再导出实际值对照。
- 过关：预测与实际在量化误差范围内；误差范围必须事先写进 `协议候选模型.md`。
- 失败：回 W3B.7，不能进地图。
- 停。

### W3B.9 [ ] AI：写成已验证地图（W4 的逆向输入）

- 谁：AI
- 依赖：W3B.8 通过
- 新建：`D:\win_game_project\12_win_上机跑\captures_freq\字节地图.md`
- 必须包含：

  - USB 接口 / endpoint
  - 包长度
  - 刷新间隔（ms）
  - 每个偏移/结构：含义（pitch / amp / 频带 / 分块 / 其它 / 未知）、取值范围、端序
  - 若能换算 Hz，写公式与证据；不能就保留 normalized pitch，禁止硬标 Hz
  - 两条完整示例和停止/归零序列

- 过关：必须注明 W3B.8 留出验证结果。
- 停。没有这张地图，W4 全部禁止。

### W3B.10 [ ] 用户：点头再进 W4

- 谁：用户读 `字节地图.md`
- 回复「地图可用」或「重抓」。AI 不得自行宣布可用。
- 停。

---

# W4 设备适配（公开运行时或已验证逆向，二选一）

### W4.0 [ ] AI：选择唯一适配路线

- 若 W3A.3 通过：选 `official`，新模块名 `official_sensa_out.py`，跳到 W4.3。
- 若 W3B.9 通过且用户 W3B.10 点头：选 `reverse`，做 W4.1。
- 两者都没有：写“W4 阻塞”，停止。不得靠猜包继续。

### W4.1 [ ] AI：逆向路线只读分类接口（还不发包）

- 谁：AI；仅 `reverse` 路线
- 先从 W3B 的枚举包、设备管理器、HID collection 列表确认目标 interface：
  - 若真是 HID collection：记录 usage_page/usage/interface_number/报告长度
  - 若是 GIP/vendor interface：明确写“不是 HID”，不要强装 hidapi
  - 记录当前绑定驱动和是否独占
- Python 包不得猜。只有确认需要的库后，经用户同意再安装；不要把 PyPI `hid` 与 `hidapi`
  混为一谈。
- 新建：`captures_freq\接口访问评估.md`
- 停。

### W4.2 [ ] 用户：批准访问方案

- HID 可直接共享访问：用户回复“允许只发已验证测试包”。
- 若需要替换驱动、WinUSB/libusb、管理员服务或会让按键失效：必须把风险写清，
  用户明确批准后才做；未批准就停止这只手柄逆向路线。
- 禁止自动替换 XboxGip/Razer 驱动。
- 停。

### W4.3 [ ] AI：实现统一 pitch 输出适配器

- `official` 路线：`hapticx\official_sensa_out.py`，封装 W3A 已验证的官方 API。
  如果公开 SDK 只有 C#/C++ ABI，可做最小 sidecar/本地进程 + Python 客户端；
  不要为了“全 Python”去调用无文档的雷云内部 DLL。
- `reverse` 路线：`hapticx\razer_pitch_out.py`，严格按 W3B.9 地图和 W4.2 批准的访问方式。
- 统一 CLI：

```
python <adapter>.py --pitch 0.1 --amp 0.5 --seconds 2
python <adapter>.py --pitch 0.5 --amp 0.5 --seconds 2
python <adapter>.py --pitch 0.9 --amp 0.5 --seconds 2
```

- 结束必须执行已验证的 stop/zero；异常/`Ctrl+C` 也要 stop。
- 不得同时调用 XInputSetState。amp 默认不超过 0.5，用户确认后上限才可到 0.8。
- “命令无报错”不等于过关，手感盲测在 W4.4。
- 停。

### W4.4 [ ] 用户：pitch 与 amplitude 独立性盲测

- SENSA 普通 A2H 关，HapticX 关。
- 第一组固定 amp=0.5，随机播放 pitch 0.1/0.5/0.9，各 3 次。
- 第二组固定 pitch=0.5，随机播放 amp 0.2/0.4/0.6，各 3 次。
- 过关：
  - pitch 组至少 7/9 次能按低/中/高排序
  - amp 组至少 7/9 次能按弱/中/强排序
  - 改 pitch 时不能只是明显强弱替代；改 amp 时 pitch 感不能乱跳
- 不震：回 W4.1/W3B.9；不要盲目加大 amp。
- 停。

### W4.5 [ ] AI：按 star 的 pitch 时间线重放

- 依赖：W4.4 两组都过。
- 使用 C2 C2 G2 G2 A2 A2 G2 的**比例**，经 freqMin/freqMax 换成 pitch_norm；
  每音 0.6s、间隔 0.1s。不要把音频 wav 直接当控制命令。
- 用户盲听是否有稳定音阶，并记录。
- 停。

### W4.6 [ ] 仅当用户点名：实验台（默认跳过）

- 不要主动做。W3 地图失败、用户才说走实验台时：另开对话，用 USB LRA 板或 DualSense 验证 F+A 管线。不是产品。

---

# W5 接回 Win 雷云

### W5.0 [ ] AI：先做实时输出性能测试

- 依赖：W4.4 通过
- 用统一适配器连续更新 30 秒：20ms 一次 A/pitch 更新；记录平均、p95、最大调用耗时、
  丢帧数、停止是否可靠。
- 过关：p95 不超过 20ms、无连续 3 帧阻塞、停止可靠。
- 若官方 API 只适合预编译效果、不适合 50Hz 实时更新：不得硬塞音频线程；
  改成分块/队列/ReplaceParametricEffect 等官方支持方式，再复测。
- 输出：`captures_freq\实时输出性能.md`
- 停。

### W5.1 [ ] AI：管线双输出开关

- 谁：AI
- 依赖：W5.0 过关
- 改 `pipeline.py` + `config_store.py`（若已有 config.json 字段就复用）
- 行为：

  - 配置键 `output: "xinput" | "sensa_pitch"`，默认 `"xinput"`
  - `"sensa_pitch"`：设备适配器发 `pitch_norm` + `amp`；低 confidence 时不更新 pitch
  - `"xinput"`：保持今天左右马达行为
  - 设备输出不得在 WASAPI 回调里做可能阻塞的 DLL/USB 调用：用有界队列 + 独立输出线程；
    队列满时丢旧帧保留最新帧，停止/异常必须归零
  - 不同时发 XInput 与 sensa_pitch

- GUI：设置页一个下拉或两个单选即可，不要重做整页。
- 停。

### W5.2 [ ] 用户：总验收 ABC

- 谁：用户
- `output=sensa_pitch`，先放 `star.wav` / 带内正弦，再放真实音乐。
- 对照 `10` 文验收表 A/B/C，填：

```
## W5.2
A 固定力度能分 pitch:  是 / 否
B 程序有 A+pitch 命令:  是 / 否
C 命令进入 Wolverine 的高级触觉路径:  是 / 否
```

- 三项都是 → 频率版 Win 雷云做完。
- 停。不要开始安卓。

### W5.3 [ ] AI：把冻结协议抄回第 10 页附录

- 谁：AI
- 依赖：W5.2 三项都是
- 只改 `10_动态触觉反馈技术.md`：
  - official 路线：记录公开 API、参数、运行时版本和适配器结构
  - reverse 路线：把 `字节地图.md` 精缩进附录 D（保留验证方法与示例）
- 里程碑 W0–W5 打勾。
- 停。

---

## 给执行模型的排错（先对号，再动手）

| 现象 | 先做 |
|------|------|
| probe 没有 A2H | 用户开雷云放音乐，重跑 W0.2，不改正则 |
| test_freq_hz 音符分不开 | 确认用了独立 6000 点估频窗+峰插值；50ms/2400点不够 |
| GUI 无 F | 查 pipeline state 键名是否与 app 一致 |
| USBPcap 列表空 | 重启；禁止 pcap_setup.ps1 |
| pcap 只有几 KB | 换 USBPcap 编号；确认手柄在这棵 USB 树 |
| tshark txt 空 | 查该 pcap 的 bus/device address 是否重枚举变化；OUT 固定 direction=0 |
| F50 与 F110 hex 全同 | 结合 3×3；禁止发明显式 Hz 字段 |
| hid 枚举不到目标接口 | 可能目标是 GIP/vendor interface，不要硬装 hidapi |
| 适配器发出不震 | 回官方路由/字节地图/驱动占用，不要改 analyzer |
| pitch 变化只表现为强弱 | W4.4 判失败；不能宣称频率控制成功 |

---

## 状态栏（执行模型每做完一号就改这里）

最后完成的工单号：_(无)_
分支（W1.6）：_(未选)_
公开 pitch 直控：未测
字节地图：无
设备适配器：未选
pitch/amp 独立性盲测：未测
