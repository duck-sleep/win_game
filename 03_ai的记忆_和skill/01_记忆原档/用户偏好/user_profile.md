## User Preferences
- Communication language: Chinese
- Virtual machine configuration modifications must be done by the user; assistant should only provide recommendations
- Git commit message editing: prefers using Vim editor over Nano
- UI design: prefers intuitive, draggable interfaces with percentage values over abstract multipliers (e.g., 1.0/0.7/0.3)
- Technical documentation: prefers plain language with analogies and avoids overly rigid, hard-to-understand content; key information (e.g., differences from reference products) should be placed in prominent positions
- Audio output: uses Bluetooth speaker instead of 3.5mm headphone