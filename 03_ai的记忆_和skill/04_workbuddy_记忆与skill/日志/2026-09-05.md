
## 00:28 播放采集静音排查（接力进行中，详细版见 .workbuddy/memory/2026-09-05.md）
- 阶段结论：静音断点比之前想的更靠下游——APM 补丁✅、Mix 注册✅、criteria 匹配✅、TeePatch 也已创建✅（AudioOut_55 上有 2 条 PatchTrack），但数据零流动（idle/FrmRdy=0/output85 standby）→ 卡在主轨→patchRecord 喂数据环节
- 待查：Tracks.cpp:1075-1100（getTeePatches 使用者）+ PatchTrack/PatchRecord 数据流；疑点 frameCountToBeReady 门控
- 测试素材：/sdcard/Music/test_tone.wav（440Hz 起伏音）；CamX HAL 循环崩溃是噪音勿误判

## 01:10 接力验证：卡点坐实"影子轨系统性不 START"（多轮干净环境实测）

### 板上包快照（重要：判包别看 ro.build.date）

- 板上已刷 9-04 全量包，四项 BSP 改动已验证三项：✅ APM 补丁生效（"keep stream 3 on mixer path" 每轮实测）、✅ HapticX priv-app 预装（/system/priv-app/HapticX，含 stereo TonePlayer + AudioRecord 工作线程修复）、✅ init.snm.usb.rc 进 vendor 且 init 已执行（persist.vendor.snm.usb_host=0 存在）
- ❌ 内核 CONFIG_JOYSTICK_XPAD_FF 仍未开（/proc/config.gz 确认）→ **正在编译的新包主要交付 FF 震动**
- **ro.build.date 还是 Aug 23（build.prop 不随增量刷新）**，判包新旧靠特征字符串（pull 回宿主 `grep -a`）

### 本轮新确认（在 00:28 会话结论之上）

- **影子轨 IDLE 是系统性的**：不止测试音——capture 注册后经 binder updateSecondaryOutputs 更新的 5 条存量系统轨（51/52/63/74/85）+ 测试音自己的影轨（110），全部 State=I/FrmRdy=0/Underruns=0，r_submix 输出线程 Total writes 0、Timestamp n=1（线程从未因活跃轨醒过）→ 影轨的 Track::start 从未被执行
- **音量干扰已排除**：STREAM_MUSIC speaker 默认 0（轨道 G db=-inf）——已 `cmd audio set-volume 3 15` 拉满后复测，peak 仍 0（音量只影响信号强度，不影响 IDLE）
- **isExternalTrack 拒绝假设排除**：PatchTrack 明确 `isExternalTrack()==false`（TrackBase.h:71），addTrack_l 不走 APM startOutput，无拒绝路径
- **板上代码 = VM 源码**：/system/bin/audioserver 二进制含全套 tee 机制字符串；git status 干净、audioflinger 无 BSP 私改 commit

## 13:20 snm970_jt 编译成功 + 虚拟耳机插孔打通（3.5mm 出口链路验证✅）

- **snm970_jt 静态编译成功**：5.15 内核 uinput write() 走 legacy 路径，必须用 UI_DEV_SETUP ioctl（write 发新结构体报 EINVAL）；编译配方存 `skills/snm970-aarch64-static-bin.md`
- **虚拟插孔测试全通**：uinput 发 SW_HEADPHONE_INSERT → WiredAccessoryManager h2w connected → openOutput WIRED_HEADPHONE ✅
- **播放链路验证**：test_tone.wav 走 DIRECT 线程 AudioOut_B5，PAL_DEVICE_OUT_WIRED_HEADPHONE open/close 干净，server 帧数推进、10s 播完移除 → **无 HAL 卡死，出口链路全通**；此前"HAL write 卡死"是 speaker 后端独有
- 板上坑：SnapdragonMusic 崩 = 缺 READ_PHONE_STATE（pm grant 修）；provision 重启丢；gamenative 弹窗抢前台 → 已 disable-user（恢复 pm enable）；无 UI 播放三连点坐标 + media_session dispatch
- 待用户主观确认：戴 3.5mm 耳机听 440Hz 测试音

## 14:00 HapticX 采集链路打通，peak=0 根因最终定位（详见 .workbuddy/memory/2026-09-05.md）

- ✅ 全通环节：r_submix 建立、每条 MEDIA 轨 nSecondary=1 tee 挂载、HapticX AudioRecord 48kHz 实时读、引擎运行中
- ❌ 根因：BSP 补丁把 MEDIA 按在 mixer 线程（为 tee），但 mixer 线程绑死 speaker 后端（永不 drain）→ 主轨 Server=0 → tee 无数据 → peak=0
- 附带实锤：audioserver 反复崩溃同源（speaker 线程卡死持主锁 → releaseAudioSessionId 死锁 → watchdog 杀，崩溃堆栈已抓到）
- 补丁位置：QSSI-irix `audiopolicy/managerdefault/AudioPolicyManager.cpp:1849` + `audioflinger/AudioFlinger.cpp:3880`
- 下一步二选一：①APM 让 mixer output 跟随路由设备 patch 到 headphone；②audio_policy_configuration.xml 直接摘掉 speaker（板无喇叭）
- 数据喂送路径已核对：主轨 releaseBuffer → `Track::interceptBuffer`（Tracks.cpp:1077）→ patchRecord->writeFrames → patchTrack framesReady ≥ frameCountToBeReady（线 816 setStartThresholdInFrames，≈2*sink+source≈6976 帧）→ isReady → 启动。但前提是影轨先 start——现状连 start 都没发生
- 静态分析见底：mix 循环（PlaybackThread::threadLoop() 内 Threads.cpp:4258）每周期对 mActiveTracks 调 updateTeePatches_l（commit pending + start），链路自洽却无效果 → **运行时条件性问题，必须插桩**

### 下一步（等正在跑的 6h 编译结束后）

1. 插桩：`updateTeePatches_l`（log has_value/mState/commit/start 返回值）、`Track::start` tee 段、`PatchTrack::start`
2. `mmm frameworks/av/services/audioflinger` 增量（分钟级）→ push **/system/bin/audioserver**（AF 核心在主二进制，libaudioflinger_*.so 只是 datapath/fastpath/timing/utils 拆分件）→ `killall audioserver` → 重测
3. AOSP 上游对照（Threads/Tracks/AudioFlinger 三文件）：VM/宿主 curl 均够不到 googlesource/github，可用 WebFetch 或 gerrit web
4. 备选怀疑点（插桩时顺带验证）：commit 时刻主轨 mState 是否恰好不在 {ACTIVE,RESUMING,STOPPING_1}；binder 更新路径对**停在 standby 线程上的暂停轨**是否永远不消费 pending（那 5 条系统轨的影轨疑似此因）

## 02:00~03:30 四轮增量修复 + 完整编译启动（深夜冲刺）

### FF 不生效的真正根因（已修复）

- GKI 内核 .config 只由 `gki_defconfig + consolidate.fragment` 合并；9-04 把 XPAD_FF=y 加进 kalama_GKI.config / kalama-consolidate_defconfig **不在合并链上 = 永远无效**（out/.config 实测 `XPAD_FF is not set`，Image 00:56 重编也没带）。已把 `CONFIG_JOYSTICK_XPAD_FF=y` 加进 `kernel_platform/msm-kernel/arch/arm64/configs/consolidate.fragment`（备份 .snm970.orig）。INPUT_FF_MEMLESS=y 本来就开。

### 播放采集修复进展（四轮增量，每轮 mmm 约 7 分钟 + push audioserver + 板测）

1. **影子轨不 START 已修复**：`Track::start` 尾部加 `updateTeePatches_l()`，pending tee patches 在主轨启动时立即 commit+start。板上实测 `patchTrack(60) start -> 0`、影轨 State=A、r_submix 输出线程醒过来写了 25600 帧（此前永远 0）。
2. **数据喂送仍未通**（剩余唯一卡点）：影轨 Server=0/FrmRdy=0 → 主轨混音数据没进 patchRecord。唯一喂送入口 `Track::interceptBuffer`（挂在 `Track::releaseBuffer`）从未执行——插桩证实混音器释放落在 `TrackBase::releaseBuffer`（约 55 次/秒，采样可见），而带截获的 `Track::releaseBuffer` 虚分发 0 次。libaudioprocessing/audioflinger 均无 BSP 私改，AudioMixerBase generic 路径理论上必经，原因未明。
3. **第四轮对策（已进树）**：TrackBase 新增虚函数 `teeIntercept()`（默认空实现），`TrackBase::releaseBuffer` 内直接调用；`Track` override → `interceptBuffer`。即基类释放路径直接挂喂送，绕开分发疑云。因板上测试环境反复翻车未及验证效果。
4. 测试环境坑（全踩过）：adb push 到只读 /system 会**假成功**（重启后必须重新 remount）；每次重启 logcat 缓冲回 256KB 且 modem 垃圾日志约 2 分钟滚满（先 `logcat -G 64M`）；引擎偶发起不来（force-stop 后重 tap）；`grep committed` 会误匹配 "committedState"；AudioMixer 里的 mTeeBufferProvider 是 BitPerfectThread 专用机制，与播放采集无关。

### 完整编译已启动（约 03:15）

- `LA.VENDOR.15.4.3-irix/build5.log`，`SNM970_A15_build.sh userdebug kalama 01`（增量+warm out/，预计 1.5~3h，含内核重编）。
- **包内内容**：APM 补丁 ✓（已验证）+ 影子轨 START 修复 ✓（已验证）+ teeIntercept 喂送修复（待验证）+ 全部 SNM970Tee 插桩 + XPAD_FF（fragment 链修复）+ priv-app 预装 + init.snm.usb.rc。
- 产物：`vendor/vendorcode/build/SNM970_userdebug_01.zip` + 解包目录 → QFIL 刷机。
- **早上流程**：① 刷包 ② `adb root; logcat -G 64M` ③ FF：`zcat /proc/config.gz | grep XPAD_FF`（应 =y）→ 接手柄（带供电 Hub，铁律照旧）`fftest /dev/input/eventX` ④ 采集：HapticX 开始捕获 + 自播测试音 → `logcat | grep -E 'SNM970Tee|输入电平'`——peak>0 即全链路收官；若仍 0，插桩日志会指明喂送断点，继续 push-audioserver 分钟级迭代（不用刷机）。
- 包验证快捷检查：板/包 audioserver 应含 'teeIntercept'、'Track::releaseBuffer entry' 字符串；内核 Image 对应 .config 应有 XPAD_FF=y（编译中途可查 `kernel_platform/out/msm-kernel-kalama-consolidate/gki_kernel/common/.config`）。

## 04:27 build5 编译完成 + 包已就位

- **产物**：`LA.VENDOR.15.4.3-irix/SNM970_EQ000_2774.C89630E57CC.9E91E72C422.91E892303_260905_100_V01_T01.zip`（3.87GB，04:27 完成，warm out/ 全程约 1h15m）+ 同名解包目录。已后台 scp 到 Windows `D:\win_game_project\22_burn_data\` 供 QFIL 刷机。
- **FF 确认**：编译中实测合并后的 GKI `.config` 含 `CONFIG_JOYSTICK_XPAD_FF=y`——fragment 修复生效，本包内核带 FF。
- 早上刷机验证流程见上文"早上流程"。

## 09:30~09:50 用户刷 build5 → FF 仍不在 + 犯错复盘

- 用户刷完 build5 实测：`/proc/config.gz` 仍是 `XPAD_FF is not set`。
- **根因：我犯了编译三大坑第一坑**——改了内核配置（consolidate.fragment 属于内核侧改动）却**没删 `device/qcom/kalama-kernel/Image`**，增量打包复用了旧内核产物。这条规则明明白白写在知识库硬约束里，属于明知故犯级失误，浪费用户一次刷机。
- **build6 已启动**（rm Image 后 `SNM970_A15_build.sh`，build6.log），这次内核必刷。
- 教训固化：**改 kernel_platform 下任何东西（DT/.ko/defconfig/fragment）→ 一律先 `rm -f device/qcom/kalama-kernel/Image`**，不能按字面理解只限"DT 或 .ko"。

## 09:50 build5 上的音频采集验证（r4 已在板上）

- md5 确认板上 audioserver = r4（teeIntercept 修复版随 build5 进了 system 镜像）。
- 插桩日志验证：影子轨链路 OK——`updateTeePatches_l pending=yes → committed 1 patches → patchTrack(59) start -> 0`。
- `TrackBase::releaseBuffer entry` 日志活跃（节流 1/256，约 5.5s 一条 ≈ 43 次/秒，来自录音轨的释放路径）。
- **但 `intercept frames`（interceptBuffer 内部日志，节流 1/128）仍为 0** → 播放轨的释放还是没有走到带截获的路径；teeIntercept 是否被播放轨触发待精确复测（下一步：数 RBCNT/`Track::releaseBuffer entry` 是否出现；若仍 0，说明播放混音释放走的根本不是 Track 对象的 releaseBuffer——下一步要在 AudioMixerBase 释放点直接加日志）。
- 小坑：`grep teeIntercept audioserver` 查二进制是**无效检查**（内联函数无字符串），有效特征串是 ALOGD 格式串（如 "TrackBase::releaseBuffer entry"）。

## 本晚错误清单（防止下次再犯，全部已付过学费）

1. **没删 Image 就改内核配置打包**（最严重，浪费用户一次刷机）→ 规则升级为"动 kernel_platform 任何文件都删"
2. adb push 到只读 /system **假成功** → 重启后必须 remount + md5 复核
3. logcat 默认缓冲被 modem 噪声 ~2 分钟滚光，好几轮测试读空 → 每轮测试前 `logcat -G 64M` + `logcat -c`
4. grep 内联函数名当二进制特征串 → 只用 ALOGD 格式串验包
5. grep 模式撞噪声（"committedState"、okhttp "intercept"）→ 加词边界或换更精确模式
6. 引擎偶发起不来（force-stop 后重 tap 才活）→ 测试前确认 status=引擎运行中再等日志
7. 一开始找错日志文件（build.log 是旧的，实际是 build4.log）→ 先问/先 ls 按时间排序

## 10:37 build6 出包（会话收尾交接）

- **build6 完成**（09:26 启动→10:37 完成，内核 09:58 全新重编，.config 含 `CONFIG_JOYSTICK_XPAD_FF=y`），zip 已搬到 `D:\win_game_project\22_burn_data\SNM970_EQ000_..._260905_100_V01_T01.zip`（**10:39 时间戳，同名覆盖了早上那个不带 FF 的——刷 10:39 这个**）。
- 精确复测确认：r4 在板上、tone 播放、影子轨 start->0，但 peak=0、`Track::releaseBuffer`/`TrackBase::releaseBuffer` 播放期间 0 次调用（仅录音轨释放）。

## ★ 播放采集终极根因（10:45 钉死，未及修复——下会话第一件事）

- **`AudioMixerOps.h`（单轨优化混音路径，grep 确认 `releaseBuffer` 出现 0 次）**：播放轨走单轨优化钩子（一个 active 轨+无重采样+无音量斜坡时选中，正是测试音/游戏的典型场景）消耗 buffer 后**从不调用 releaseBuffer** → 挂在 Track::releaseBuffer 上的 `interceptBuffer`（tee 喂送唯一入口）永不执行 → 影子轨永远无数据 → 采集静音。整条"secondary outputs 影子轨不激活"的因果链到此闭环：APM 层（已修）→ 影子轨 start（已修）→ **喂数据入口不存在（此为最终根因）**。
- **下会话修复方案（推荐 b）**：
  a) 小修：在 AudioMixerOps.h / AudioMixerBase 单轨路径消耗后补 releaseBuffer 调用（改动最小但要想清楚释放语义）；
  b) **绕开混音器：在 `MixerThread::threadLoop_mix()`（Threads.cpp ~5496）混完全部轨后，把 mSinkBuffer 按"有 tee patches 的活跃主轨"喂给它们的 patchRecord**——必经之路无绕行；注意按轨的 usage 过滤（mix 规则只收 MEDIA/GAME/UNKNOWN），避免把铃声等混进去；音量用轨的最终音量近似即可（HapticX 用途可接受）。
  c) 修完 mmm audioflinger 增量 → push audioserver → killall → 测 peak（分钟级迭代，不用刷机）。
- VM 内全部改动（均有 `.snm970.orig` 备份）：QSSI `AudioPolicyManager.cpp`（APM 补丁）、`Tracks.cpp`（影子轨 START 修复+插桩）、`AudioFlinger.cpp`（插桩）、`TrackBase.h`+`PlaybackTracks.h`（teeIntercept，此路线已证死路可回退）、VENDOR `consolidate.fragment`（XPAD_FF）。
- 本地留档：`.workbuddy\tmp_apmcheck\`（audioserver r1~r4、apply 脚本、af dump、日志）。
- build6 包内音频状态：APM 补丁 ✓ + 影子轨 START ✓ + teeIntercept（无效但无害）——**采集仍未通，等下一版 push 迭代**；FF 稳。

## 11:30 采集静音"终极根因"被推翻并钉真凶：HAL write 卡死（重大反转）

### 推翻昨晚 10:45 的结论
- "AudioMixerOps.h 单轨路径无 releaseBuffer"**查错了文件**：单轨 hook 真身在 AudioMixerBase.cpp（process__oneTrack16BitsStereoNoResampling:1321 / process__noResampleOneTrack:1503），每圈必调 releaseBuffer；重采样路径由 AudioResampler 负责 release（394/444/490/540）；provider 确认就是 AF Track 本尊（asExtendedAudioBufferProvider 返回 this，AudioMixer.cpp:340 链上无包装器命中）。**所有混音路径都会释放，"releaseBuffer 不被调"不是根因。**

### 板上实证（严谨流程复测：volume 3=15 已设、r4 md5=32930f50 核对一致、capture+放音后）
- dumpsys media.audio_flinger：音乐轨 57（48k/16bit/stereo，AudioOut_D）**Server=00000000（混音器从未消费）**，FrmCnt=3844 满（app 灌满 80ms 后永久阻塞写入）
- **AudioMixer tracks: 空**——prepareTracks_l 从未使能该轨
- AudioOut_D：**Total writes=4（几分钟不增长）、Blocked in write: yes、Timestamp err=5**——FastMixer 卡死在 HAL write， DSP buffer 灌满 4×960 帧后再不 drain
- 因果链钉死：**HAL write 永久阻塞 → 输出线程冻结 → 音轨永不消费 → releaseBuffer/interceptBuffer 永不执行 → 影子轨无数据 → 采集静音**。之前"影子轨 idle/FrmRdy=0"全是下游症状
- 内核侧干净：kalama-qrd-snd-card 正常注册（8 进 8 出），dmesg 音频驱动零错误；PAL 仅 "Unsupported device id 0"（speaker 连接探测，无害但印证无喇叭）
- 板子无喇叭 + ADSP 充电栈固件残缺前科 → speaker 后端无法 drain（要么无喇叭 BE 起不来，要么 ADSP 音频 session 固件又缺）

### 下一步方向（三选，等用户决策）
1. **插 USB DAC/耳机（带供电 Hub + 切 host）**：给音频一个能 drain 的真出口；USB audio 若直通不过 ADSP 则整条采集链路立即可验证——最便宜
2. **深挖 HAL/DSP write 卡死**：抓 AGM/PAL session 日志、查 spkr protection/WSA macro 状态——BSP 向，可能是又一个固件残缺（参照 UCSI 案例），工作量未知
3. 接受本板不能出声，采集验证降级为"r_submix 自环"注入式（不真实，仅开发用）

### 其他
- cmd audio set-volume 3 15 生效（dumpsys Stream volumes 3:0dB），G db=-inf 是因轨从未被混音（静态显示），不是音量没生效
- 测试流程复用：force-stop → appops PROJECT_MEDIA allow → 启动 → tap(960,855) → 无弹窗直接开始捕获 → VIEW intent 放 /sdcard/Music/test_tone.wav

## 11:55 板载蓝牙确认可用（采集验证新出路）
- 蓝牙栈 ON（state: ON，开机即启用，MAC XX:XX:XX:XX:2E:3D），HAL android.hardware.bluetooth@1.1-service-qti 正常
- 芯片 soc = hamilton（QCA/WCN 系，kalama 平台标配）；rfkill bluetooth 正常
- 无已配对设备；SCAN_MODE_CONNECTABLE；Settings 界面没有 devicepicker activity，配对需走系统设置 UI 或 bt 命令
- 意义：蓝牙音箱 A2DP 是独立于 speaker BE 的输出路径，可作为音频 drain 出口解 HAL write 卡死 → 采集链路可验证（替代 USB DAC 方案）

## 12:05 Settings 进不去修复（根因：设备未 provisioned）
- 现象：am start Settings 显示 ok 但焦点秒回桌面；Settings 进程日志 "Device is not provisioned, exiting Settings"
- 根因：这台掌机 ROM 没走完成机向导，SettingsHomepageActivity 检测到未 provisioned 就直接退出
- 修复（已生效）：settings put global device_provisioned 1 + settings put secure user_setup_complete 1（重启可能丢失，若复发重写即可）
- 其他发现：
  - 桌面是定制 IRIX 前端（app.gamenative/com.onion.launcher），顶栏自绘（时间/搜索/WiFi/BT 图标），SystemUI 被盖住→下拉不了是正常的
  - "Scan for ROMs?" 弹窗是 launcher 自己的（无 gamelist.xml 时反复弹），点 Not now 可关但可能再弹
  - 面板物理分辨率 1080x1920 竖屏横用；input tap 坐标注意：screencap 图是缩放+旋转的，直接按图上比例点会偏（踩过两次）
  - Settings 修好后是竖屏布局渲染
- 后续：用户可自己进 Settings 连蓝牙音箱/WiFi；BT hamilton soc 正常 ON

## 推游戏到掌机 SSD（13:00~13:15）

- 用户要求把《寄生前夜 第三次生日 汉化版.iso》（1.47GB）推进掌机 SSD。
- **记忆考古**：8-27 TRAE 会话记录确认过先例——SSD 在安卓里显示为「安卓」盘，游戏放 `GAMES\` 目录，PPSSPP 从那里加载。
- 设备 WiFi adb **192.168.31.13:5555**（IP 又变了，旧 .93 作废）；SSD 卷现挂 `/mnt/media_rw/7FEE-F970`（exFAT，adb root 可写）。

## 16:30~17:00 TRAE 接力：APM 设备强制补丁实施（晚间冲刺）

### 基线复测（板上，改码前）
- 设备改走 **USB adb（序列号 12344321，C:\platform-tools\adb.exe -s 12344321）**，WiFi adb 192.168.31.13 offline
- 耳机已连接（Connected devices: type:0x8 headphone h2w）——用户买的 3.5mm 耳机已插
- 复测流程：音量 3=15 → force-stop HapticX → appops → 启动 → tap(960,855) 捕获 → VIEW 播 test_tone.wav
- 结果：capture 注册 ✓、SNM970Tee(66) 日志活跃（pending=no 已 commit、state 6→3→4 迁移中）但 **AudioOut_D 仍绑 SPEAKER(0x2)、Total writes=4 卡死** → 与 13:30 结论一致，坐实最后断点

### APM 补丁（第二刀，在 VM QSSI-irix 上）
- **位置**：`frameworks/av/services/audiopolicy/managerdefault/AudioPolicyManager.cpp` 的 `getNewOutputDevices()` 顶部（7818 行起，null 检查后）
- **逻辑**：playbackCaptureActive（loopback render mix 注册中）+ 输出非 policyMix（排除 r_submix）+ sharesHwModuleWith(mPrimaryOutput)（只管 primary 模块）+ WIRED_HEADPHONE/HEADSET 在 availableOutputDevices → 强制返回耳机设备，ALOGD 带 "playback capture active, output" 特征串
- **双时机覆盖（源码验证）**：
  1. 音乐轨启动：`startSource()` 首条活跃轨时 `devices = getNewOutputDevices(...)` → `setOutputDevices()` 应用 → 覆盖"先捕获后播放"
  2. 注册 mix 时：`registerPolicyMixes()` 尾部 `checkForDeviceAndOutputChanges(); updateCallAndOutputRouting();` —— 后者对**所有输出**（含空闲）重路由 → 覆盖"先播放后捕获"
- 本地留档：`25_xbox_control\bsp_patch_snm970\AudioPolicyManager.cpp`（scp 双向同步）
- 产物模块：**libaudiopolicymanagerdefault.so**（630KB，/system/lib64/，板上已确认存在同版本）

### 编译踩坑（QSSI 树增量编译三连坑）
- 坑1：`lunch qssi-userdebug` 两段式 → `release_config.mk:270: No release config set`（AOSP15 新机制要求 TARGET_RELEASE）
- 坑2：`export TARGET_RELEASE=` 空串绕过 → `version_util.mk:167: Argument missing`（release 没加载导致 PLATFORM_SYSTEMSDK_MIN_VERSION 空）
- 坑3：`lunch qssi-trunk_staging-userdebug` 三段式 → dumpvars 仍报同样错（lunch 的 dumpvars 阶段不认）
- **解法**：绕开 lunch 直接 soong_ui：`export TARGET_PRODUCT=qssi TARGET_BUILD_VARIANT=userdebug TARGET_RELEASE=trunk_staging TARGET_BUILD_TYPE=release && build/soong/soong_ui.bash --make-mode MODULES-IN-frameworks/av/services/audiopolicy`
- out/soong/release-config/ 里 `release_config--trunk_staging.varmk` 的存在证明 trunk_staging 是有效 release 名
- **新坑**：`adb push <文件> <目录/>` 目标带尾斜杠会报 `remote couldn't create file: Is a directory` 且**假成功**（显示 1 file pushed + 速度统计但实际没写）；目标必须写完整文件路径（如 `GAMES/xxx.iso`）。
- 重推成功：`/mnt/media_rw/7FEE-F970/GAMES/寄生前夜 第三次生日 汉化版.iso`，大小 1466406912 字节，**md5 两端一致** `3d36eec44702f8e455a237074fbb8645`。GAMES 里旧《极魔界村》game.iso 还在。
- adb shell 里看不到 `/storage/7FEE-F970` 用户视图，但 PPSSPP 有存储权限走 app 视图，不影响加载（8-27 已验证）。
