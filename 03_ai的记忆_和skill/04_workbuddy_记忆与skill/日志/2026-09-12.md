# 2026-09-12 — 安卓雷云闭环 + AetherSX2 补装

## 一句话
板上 HapticX **0.1.12** 已能偷听系统声并震手柄；薄低音要切 **动态**。下次 inc 大包会带上 0.1.12 预装 + A2DP 策略 + ff_bridge 开机。Git 仓里的 App 源码还是 0.1.0。AetherSX2 当天丢了，已从 E 盘 APK 装回。

## 雷云（用户验收要点）

- 采集：AudioPlaybackCapture（MediaProjection，`PROJECT_MEDIA allow`）。REMOTE_SUBMIX 本板经常 peak=0。
- 自播只放 `tone60`（60Hz+880Hz，5s），马达跟采集，不再本地假 60Hz。
- 四档：受控 / 均衡 / 动态 / 自订 + 三柱 0–200%。
- 用户晚间：SSD `GAMES/jade-garden-serenade.mp3` 在均衡不震，**改动态后震了**（门限 0.02、超低音 130%）。
- 停震：静音 peak 门限、`forceZero`、通知「停止震动」、划掉任务。零强度必须 `EV_FF=1`。
- 桥：root `ff_bridge` 读 logcat `HapticXOut` → Xbox `eventN`（插拔会变；别发到 hv-haptics）。

## 大包（源已改，用户自己编）

```bash
cd ~/Desktop/10_code/LA.VENDOR.15.4.3
./SNM970_A15_build.sh userdebug kalama 01 inc 2>&1 | tee build.log
```

不必删 Image。会编 QSSI（带新 HapticX.apk）+ vendor（策略 + hapticx_ff）。

| 位置 | 内容 |
|------|------|
| `LA.QSSI.15.0/vendor/meig/HapticX/HapticX.apk` | 0.1.12（旧 stub 备份 `.0.1.0.bak`） |
| `kalama.mk` | `a2dp_offload.disabled=true` + `hapticx_ff/` COPY |
| 策略 XML | md5 `7be27531…` / `4e472f90…` |

刷完：`versionName=0.1.12` 应在 `/system/priv-app/HapticX`。

## Git

`D:\win_game_project` 工作树干净，分支 `cursor/setup-cloud-agent-environment-72e4`。0.1.12 Kotlin **不在仓里**，只在板子 APK 和 VM 预装文件。

## AetherSX2（21:35）

- 不见了：`pm` 无 `xyz.aethersx2.android`。PPSSPP / OnionKnight 还在。
- 已装回：`E:\@【已整理】经典游戏rom\roms\ps2\AetherSX2-v1.5-3668.apk`
- 内部 `/sdcard/PS2/bios` 空了，已从 SSD `PS2/bios` 拷回。
- `am start` 后在 SetupWizard。OnionKnight 主页没有 sideload 图标，所以用户「看不见」。
- 战神2 CHD 仍在 SSD `GAMES\`。

## 验收

照 `12_win_上机跑\14_雷云验收_0912.md`。应急停震：`am force-stop com.meig.hapticx` + `ff_rumble eventN 0:0`。

---

## 第四轮（21:50–23:40）——条为 0 → 顶满 → 改回 Windows + 悬浮条 + 固件核源

用户原话：左/右马达还是 0；切 App 看不出变化；悬浮看得见但好像跑满；Windows 是不是也这样；先存记忆，看直接编固件刷进去能不能用。

### 板上迭代
| 版 | 现象 / 改动 |
|----|-------------|
| 0.1.13 | 采集有声（peak 0.12–0.47），30–130Hz 算出 1–6%，UI `toInt()` 显示 0 |
| 0.1.15 | 用整段 PCM 映射马达，条能看见，但游戏一出声就 60–90% 顶满 |
| 0.1.16 | 加 `TYPE_APPLICATION_OVERLAY` 悬浮条（可拖），Manifest 可分屏。用户确认看得到悬浮 |
| 0.1.17 | **撤 PCM 顶满**，映射回到 Windows `analyzer.py`：gamma 压缩、固定 `transient_vol`、左=body+0.7t / 右=t+0.4body、gain=0.67。只留 `CAPTURE_SCALE=2.5`。用户说「好像还行」 |

Windows **不是**跑满：只看 30–130Hz；右马达跟节拍脉冲（均衡 35% / 动态 60%）。

### 固件源（23:40 核过，APK 已换成 0.1.17）

不必删 Image。用户自己：

```bash
cd ~/Desktop/10_code/LA.VENDOR.15.4.3
./SNM970_A15_build.sh userdebug kalama 01 inc 2>&1 | tee build.log
```

| 项 | 位置 | 状态 |
|----|------|------|
| inherit | `LA.QSSI.15.0/device/qcom/qssi/qssi.mk:310` `vendor/meig/hapticx.mk` | ✅ |
| 预装 APK | `vendor/meig/HapticX/HapticX.apk` **0.1.17** | ✅ 刚换（`.0.1.12.bak`） |
| A2DP 属性 | `device/qcom/kalama/kalama.mk` `a2dp_offload.disabled=true` | ✅ |
| 策略 XML | kalama `audio_policy_configuration.xml` md5 `7be27531…`；common `bluetooth_qti_hearing_aid_…` md5 `4e472f90…` | ✅ |
| ff_bridge | `device/qcom/kalama/hapticx_ff/` ELF aarch64 static；开机 `sys.boot_completed` | ✅ |
| appops | 同 rc：`PROJECT_MEDIA` + `SYSTEM_ALERT_WINDOW` allow | ✅ 刚加 |

刷完会有 App + 桥 + A2DP 策略。**仍要**点一次「开始捕获」（系统 MediaProjection 弹窗没法预装）。`hapticx_ff.rc` 的 `seclabel u:r:su:s0` 只适合 userdebug。Git 仓 Kotlin 仍是 0.1.0，大包不依赖仓里那份源码。
