# 2026-09-03 工作日志

## 会话重启：上下文重建 + 记忆体系落地（上午）

- 用户说"上一个任务挂了，重新开始"。本轮先做上下文重建与归档整理，未改动任何项目代码。
- 通读并确认可用资料：
  - `03_ai的记忆_和skill\02_知识库_整理版.md`（核心恢复手册，10 章，含环境拓扑/编译三大坑/SSD 修复/设备树规范/提交规范）
  - `03_ai的记忆_和skill\04_workbuddy_记忆与skill\MEMORY.md`（WorkBuddy 增量记忆，9-02 版）
  - `04_ai和运行资料\04_ai资料\`（01 工程说明、02 快速编译指令、05 22.04 环境操作清单、10 同事建议、11 安卓工程教程、20 USB 排查）
  - `04_ai和运行资料\05_烧录说明.txt`（QFIL 步骤 + 待办：MTP 不通、vold 打通 SSD/SD 自动挂载）、`10_虚拟机联网问题.txt`（双网卡：仅主机跑 SSH + NAT 出外网）
  - `.workbuddy\memory\2026-09-02.md`（昨夜三条：OTG 根因与补丁、host 模式绕过实验、HapticX UI 重构）
- 确认昨日（9-02 深夜）最后状态：HapticX 在做 profiles 页签 UI，`profiles_shot.py` + `ui_preview_profiles.png`（23:59）已产出；`config.json` 已写入 `mode_band_gains` 四模式增益（dynamic/balanced/controlled/custom），说明模式增益保存已打通。

## 记忆体系调整（本次落地）

- WorkBuddy 记忆主存放地正式定为 `03_ai的记忆_和skill\04_workbuddy_记忆与skill\`，新增 `日志\YYYY-MM-DD.md` 分层（长期记忆 MEMORY.md + 每日日志分离）。
- 新建项目 skill：`skills\snm970-context-recover\SKILL.md`（新会话三步恢复流程 + 工作约定 + 环境速查）。
- `.workbuddy\memory\` 降级为指针，不再承载正文。

## 雷云项目会话报错弃用（10:21，收尾）

- 用户从"雷云项目"会话切过来（报错 Error 11135：图片格式不支持 / "start a new conversation, replace the image"）。该报错是**服务端拒收坏图、污染整个会话上下文**，客户端无解，正确做法=弃用该会话、开新会话（用户已这么做）。
- 排查确认：`03_ai的记忆_和skill` 与 `04_ai和运行资料` 两个目录下**没有任何图片文件**，不存在可删的磁盘坏图，无需删除操作。
- 约定补强（用户重新明确）：记忆与 skill 放 `03_ai的记忆_和skill`；AI 运行/资料放 `04_ai和运行资料`。已在 skill `snm970-context-recover` 的记忆约定里把 `04_ai和运行资料` 补为恢复资料来源。
- 教训（写进知识库备用）：对话里别上传格式不支持的图片（HEIC/特殊编码/损坏图），一旦被服务端拒收会连带整个会话报废，只能弃会话重开。

## 讲解"算法实现不一样" + 文档改写（10:25）

- 用户读 `12_win_上机跑\09_动态触觉反馈技术.md` 第 ② 节没看懂。
- 结论：**参数（配方）从雷云日志抄到了，算法（公式）是黑盒拿不到**，我们照着参数自写实现 → 同类不同源，不是另起炉灶。三处差异：谱通量节拍检测自研（`_detect_transient`）、压缩器缺 `amp_comp`、输出是单一标量而非"3 频段 + 2 瞬态"多通道流。
- **新发现（可动手的点）**：`analyzer.py` 压缩公式 `min((能量/0.3)**(1/gamma),1)*0.67`，**能量 ≥0.3 就削顶**——0.3 / 0.5 / 1.2 三档输入全部输出 0.67，中等音量以上强弱层次完全丢失。这解释了实测表里三档峰值都卡在 0.5~0.67 的现象。补 `amp_comp` 软拐点或抬高 REF_LEVEL 可缓解（低成本，未动代码）。
- 已把白话解释 + 削顶数值表写回 `09_动态触觉反馈技术.md` 第 ② 节，并在"还没做"清单里给 amp_comp 条目补上具体后果。

## 雷云复刻项目"一看就挂"根因修复（10:24~10:35）

- 现象：重开会话后，AI 一去看雷云复刻项目（`25_xbox_control\hapticx\`）就报 11135"图片格式不支持"、整会话报废。
- 根因定位：`hapticx\ui_preview_profiles.png`、`ui_preview_dynamic.png`（9-02 晚 `profiles_shot.py` 产出）是坏图——脚本 `chunk()` 用 `<I` 小端打包 PNG chunk 长度/CRC，PNG 规范要求大端 `>I`，IHDR 长度字段变成 0x0D000000，任何解析器拒收。
- 修复动作：
  1. `profiles_shot.py` 已改 `>I`（脚本修正，下次截图产出好图）
  2. 两张坏 PNG 用修正逻辑从同内容合法 BMP 原地重新生成（1116x819），坏图已不复存在
  3. 全工作区 PNG 块结构+CRC 复验：全部 OK（含 `_razer_ui_ref\sensa1/2.png`、`ui_live.png` 等）
- 知识沉淀：踩坑记录已写入 `04_workbuddy_记忆与skill\MEMORY.md`；诊断方法固化为 skill `hapticx-png-guard`。

## HapticX 频段增益"看不见"布局修复（10:42~10:55）

- 现象：动态模式下"频段增益"编辑器整个看不见（根因与模式无关，任何模式都会）。
- 三个叠加原因：①profiles 页内容自然高度 ~850px 超过窗口可视区且无滚动，编辑器卡排最后被整体裁出窗外；②`BandGainCanvas` 构造时写死宽高、不响应 resize；③画布 `pack(anchor="w")` 不带 expand，窗口再大也不长。
- 修复：
  1. `band_gain_canvas.py` 加 `<Configure>` 自适应重绘（尺寸变化→重算 pw/ph→重绘）
  2. `app.py` profiles 页改用 `_make_scroll_page()` 滚动容器（Canvas+Scrollbar，矮窗口可滚动）；频段画布 `fill+expand` 吃满剩余空间；窗口默认 1100x780→1100x860
  3. 滚轮用 `bind_all + winfo_containing 指针归属判断`（子控件上 Enter/Leave 会误触发解绑，不能用）
- 验证：`profiles_shot.py` 实测截图，动态模式绿 100% / 青 70% 拖拽把手与半透明区域均可见，右侧滚动条正常；预览图已更新。

## 频段边界可调（对标雷云拖动音频输入范围）（10:54~11:10）

- 需求：雷云里每个频段的"音频输入范围"可拖可调，本项目原来写死 BAND_EDGES=[30,70,100,200] 只读。
- 全链路打通（4 处）：
  1. `analyzer.py`：`band_edges_for(config, mode)` 取每模式边界；A2HAnalyzer 实例级 `band_edges`，`set_band_edges`/`reconfigure` 实时重建 eq_table（外边界 30/200Hz=触觉马达物理范围固定，内边界可调，相邻边界最小间隔 10Hz）
  2. `pipeline.py`：`set_band_edges`（锁保护、记入 config `mode_band_edges` 按模式保存）；`set_mode` 切换时随增益一起加载
  3. `band_gain_canvas.py`：内边界可拖拽（命中 ±6px、拖动时线上显示当前频率值、悬停变左右箭头光标）、`_fx_inv` 像素→频率反变换
  4. `app.py`：最小值/最大值 Spinbox 改为可编辑（首段下界/末段上界保持灰色固定），改值/拖动/切模式/重置全同步；重置按钮连边界一起还原
- 附加修复：`capture_window.find_window` 抓窗口改为"可见窗口里面积最大"（曾抓到 160x28 幽灵窗口）。
- 验证：拖拽夹紧逻辑单测（拖过头→夹到相邻边界±10Hz）、`test_analyzer.py` 全过、1116x1100 全页截图确认数值框/边界把手/频率刻度全部正常。

## 安卓雷云技术方案文档（11:52~12:10）

- 新建 `12_win_上机跑\10_安卓雷云技术.md`：把 HapticX 搬上 SNM970 掌机（OTG XInput 手柄跟着游戏声音震）的工程方案。
- 核心结论：**引擎 system 化 + UI 独立化 = priv-app**，不深改 Settings/framework（Settings 做不了频谱图/拖拽编辑器，迭代 6h+）。
- 用户拍板：①先外接手柄、掌机 LRA 马达以后再加；②独立雷云界面+Settings 简版开关；③先内部工具后产品化；④音频来源=监控设备当前输出（等同 loopback），具体路径待实测。
- 音频捕获三路：AudioPlaybackCapture（有弹窗+opt-out 风险）→ priv-app 特权 → AudioFlinger/HAL 环采补丁（兜底大招）；对上层透明。
- 输出：xpad 内核驱动 + evdev FF_RUMBLE ioctl（需确认 CONFIG_JOYSTICK_XPAD(_FF)，SELinux 加规则）；绕开 framework Vibrator。
- 参数迁移：config.json 两端共用 schema；验收=DSP 对拍（同 wav 两端输出逐帧一致）。
- 分阶段：阶段0=OTG 收官（即当前 ADSP/UCSI/VBUS 那条线）→ 阶段1=DSP 移植对拍 → 阶段2=priv-app 产品化 → 阶段3=可选 HAL 下沉。

## 安卓雷云方案 v2 落地版（14:47~15:00）

- 用户指路：源码位置先看记忆（02_知识库_整理版.md §二）——`~/Desktop/10_code/LA.VENDOR.15.4.3`（vendor/内核主入口）+ `LA.QSSI.15.0`。教训：SSH 探索前先翻记忆手册，别全盘 find。
- VM 实地核实（ssh）：xpad.c 已在 `kernel_platform/msm-kernel/drivers/input/joystick/`，`CONFIG_JOYSTICK_XPAD=y` 已开（kalama-consolidate/gki 两个 defconfig）；**CONFIG_JOYSTICK_XPAD_FF 没开**——需加一行。
- privapp 权限范式：`device/qcom/qssi/privapp-permissions-qti.xml`。
- `10_安卓雷云技术.md` 升级为 v2：工程两端分离（app=Gitea 独立仓 `hapticx-android`，Windows 开发；glue=VENDOR 树 ≈6 文件点）；日常开发 adb install 分钟级，6h 编译只用于 glue/验收；FF defconfig 改动不阻塞 OTG 复测（两线并行）。
- 待用户 review 方案 v2。

## 安卓雷云 app:发现既有工程 + 修复编译出 APK（19:55~20:15）

- 用户指示:手柄先不管（手柄还没到手），先做 app + 音频捕获验证。
- **重大发现**:挂掉的"雷云项目"会话已在两处留下半成品:
  - 本地 `25_xbox_control/hapticx-android/`（Gradle 工程,阶段1大半完成:A2HAnalyzer 全算法/2400点Bluestein FFT/PlaybackCaptureEngine/HapticsOutput LogOutput/雷云风调试UI 四模式切换）
  - VM 同名目录副本 + Windows 工具链已装好（`D:/android_build/`: jdk17 + SDK34 + gradle-8.7）
  - **我一度重复造轮子**（写了平行 AOSP 结构）,已全部清理（本地 rm + VM git rm 两次提交还原）
- 编译修复（上个会话遗留 5 处 Kotlin 错误）:
  1. MainActivity `EXTRA_RESULT_DATA` 未限定 → `HapticXService.EXTRA_RESULT_DATA`,并把 resultCode 一起塞进 bundle
  2. HapticXService `getMediaProjection(Intent)` 是 API35 才有 → 改双参 `getMediaProjection(resultCode, resultData)`;补 MediaProjection import;registerCallback handler 传主线程 Handler
  3. SpectrumView `viz[i]*h` Double→Float 收窄
- 环境坑:SDK 路径写 `local.properties`（sdk.dir=D:/android_build/android-sdk）;C 盘 `~/.gradle/.tmp` 写缓存"拒绝访问"→ 保持默认 GRADLE_USER_HOME、杀 daemon 重试即过
- **BUILD SUCCESSFUL** → `app/build/outputs/apk/debug/app-debug.apk`（7.3MB）。adb devices 无设备——等用户接掌机 `adb install -r` 验证音频捕获（游戏 opt-out 实测）
- 修复的三个源文件已同步到 VM 副本。

## DSP 对拍收官:抓出加窗 bug + CROSSCHECK PASS（20:15~20:25）

- 按 README 待办做"DSP 对拍 + 数值单测"（不需要硬件）:
  - 基准生成器 `hapticx/tools/crosscheck_gen.py`:确定性鼓点+正弦信号,动态模式+自定频段增益/边界,60 块逐块写 fixture CSV
  - Kotlin 重放 runner `dsp/CompareMain.kt`（保留在工程内,运行命令写进 README）
- **对拍抓出移植 bug**:Kotlin `eqWeightedBand` 忘了乘汉宁窗（Python 是 `rfft(buffer*window)`）→ 修复后 60 块对拍 PASS:左右马达偏差 ~5e-13、level ~5e-14、瞬态序列逐块一致、beat_count 一致;viz 偏差 8e-6（Bluestein vs numpy 的双精度底噪,可接受）
- 重新出 APK（含修复）并同步 A2HAnalyzer.kt 到 VM 副本;README 待办更新（剩:频段增益编辑器 UI、真机验证）
- 待用户接掌机实测音频捕获。

## 磁盘占用盘点：安卓工具链/缓存都在哪（22:24）

- 背景：用户问"磁盘小，刚刚装了什么、装在哪"。本会话**没有新装任何软件**，只是编译触发了 Gradle 缓存下载。
- **D 盘 `D:\android_build\`（1.5G，之前会话装的整套工具链）**：
  - 必须保留：`android-sdk` 422M、`jdk` 303M、`gradle-8.7` 143M
  - 可删残留（合计 618M，**已列清单等用户确认**）：`downloads` 325M（安装原始 zip）、`gradle.zip` 128M、`gradle-home` 165M（昨天 GRADLE_USER_HOME 迁移失败留下的重复缓存）
- **C 盘唯一增长点**：`~/.gradle/caches` 805M（编译自动下载的 AGP/Kotlin/androidx 依赖，**不建议删**，删了下一次编译重下 800M）；`~/.android` 6M 可忽略
- 工程本体 `25_xbox_control/hapticx-android/` 含 build 产物仅 27M
- 注意：若 C 盘告急删 `.gradle/caches` 也可，代价是下次编译重下。

## OTG 复测突破：强制 host + 带供电 Hub 全链路打通（22:30~22:45）

- 用户重新开机测试 USB 口：带供电 Hub（VIA Labs 4 口）+ 读卡器，读卡器"灯不亮"。
- **操作顺序教训**：第一次切 host 时 adb 走的正是被切的 USB 口，命令一出连接即断；重启板子后按新流程——USB 连着时先 `adb tcpip 5555` 开 WiFi adb（板子 WiFi 重启后掉线需 `cmd wifi connect-network 今天鸭鸭还好困 wpa2 00000000` 重连，DHCP 新 IP **192.168.31.93**，旧 .180 作废），拔线插 Hub 后走 WiFi 切 host。**以后凡是动 USB 模式，必须先确保 WiFi adb 在线**。
- `echo host > /sys/bus/platform/devices/a600000.ssusb/mode` 后：
  - **Hub 枚举成功**（usb 1-1，VIA Labs USB2.0 Hub 4 口，`bMaxPower=0mA` = 自供电，完全绕开板子 VBUS=0 的死结）
  - **读卡器枚举成功**（usb 1-1.1，14cd:1212 Mass Storage）——灯不亮只是因为读卡器灯仅读写时闪，实际有电
- 初始探测阶段有 usb-storage 无限 reset + I/O error（hostbyte=0x07），约 30s 后自行稳定；`sdi`/`sdi1`（~58G FAT 卡）出来，vold 自动挂载 `/mnt/media_rw/0123-4567`
- 稳定性验证：64MB 连续读 9.4MB/s 零错误
- **结论：OTG 数据链路 + 供电问题一并解决**。手柄（总线供电）插此 Hub 即可枚举（CONFIG_JOYSTICK_XPAD=y 已在内核），**就差手柄到手**。
- 遗留：强制 host 不持久（重启回 peripheral），后续 glue 工作需加 init/vendor 脚本开机自动 `echo host`；初始 reset 抖动可后续观察。

## 手柄 2.4G 收发器枚举成功——阶段 0 收官 + 方案文档重写（22:47~23:10）

- 用户把雷云手柄 2.4G 收发器（**Razer VID_1532&PID_0A3F**，电脑识别为"Xbox 游戏设备"/XboxComposite）插入 Hub：
  - **手柄关机时收发器不握手**（Hub 连 connect 事件都没有，像坏的一样）——排障大坑，已写进方案铁律
  - **手柄开机后秒枚举**：`usb 1-1.1 idVendor=1532 idProduct=0a3f` → **xpad 驱动绑定** → `input5`/"Generic X-Box pad" → `/dev/input/event5`
- 设备能力全量验证（getevent -pl）：全部按键+双摇杆+双模拟扳机（ABS_Z/RZ 0-1023）+十字键——标准 XInput 全映射
- 按键事件流验证未完成：90s/300s 两个 getevent 窗口都没抓到事件（怀疑手柄休眠或按键时间没对上），**待下次继续**
- `10_安卓雷云技术.md` 全文重写：阶段 0 ✅收官、阶段 1 ✅完成（DSP 对拍 PASS+APK）、阶段 2 🔄当前主战场；三条铁律（动 USB 先开 WiFi adb / 供电 Hub 绕过 ADSP / 收发器要手柄开机）；下一步计划三梯队（零编译：按键验证+APK 装机测音频捕获 opt-out；攒一次刷机：FF 宏+自动 host+预装；产品化：evdev FF 输出+编辑器 UI）。

## APK 装机实测音频捕获（深夜，核心调试）

- APK 装机成功（WiFi adb 192.168.31.93:5555），授权链路反复折腾后全通：MediaProjection 授权→服务前台→采集引擎 48kHz mono float 960/块跑起来
- **坑 1：PPSSPP 全屏抢焦点**——用户点的是游戏不是 app；`am start -f 0x10840000` 才能压过
- **坑 2：布局溢出**——480dpi 下竖排元素把"开始"按钮挤出屏幕（按钮 y~905 > 可视 936 且被导航栏裁掉），套 ScrollView 修复重编
- **坑 3：AppOps PROJECT_MEDIA=ignore**——反复 stop/start 授权后 appop 被记成 ignore，AudioRecord 被系统静默静音（不报错只返 0）；`appops set com.meig.hapticx PROJECT_MEDIA allow` 修复后 SystemUI 走免确认自动批准路径
- **坑 4：屏幕睡眠导致"点击无反应"**——`input keyevent KEYCODE_WAKEUP` 唤醒
- **坑 5：无扬声器盲调**——板子没喇叭，靠 `dumpsys audio | grep state:started` 确认音源在播；stagefright 不存在；AOSP Music app (com.android.music) + 推 wav 到 /sdcard/Music + MEDIA_SCANNER_SCAN 可造测试音源
- **根因定位（工具链：uiautomator dump / dumpsys appops / dumpsys media.audio_flinger / dumpsys media.audio_policy + VM 内 AOSP 15 源码对照）**：
  - 采集链路 100% 正常：DPM mix 注册正确（MIX_TYPE_PLAYERS + LOOP_BACK|RENDER，匹配 UNKNOWN/MEDIA/GAME=OR 语义）、r_submix HAL 路由配对（route[9] addr=902246461:ap:8mixp:0 rate=48000）、录制线程活跃且 Sil=n 未被静音
  - **断点：secondary outputs 影子轨不激活**——io 149（capture 输出）上 15 条 audioserver 影子轨全部 idle/0 帧，主轨（Music/PPSSPP，targetSdk 33/29 均≥29 可采集）发声时从不级联；HAL Frames written: 0 → 采集拿到纯静音
  - 结论：QCOM BSP 的 AudioFlinger/APM secondary outputs 激活链路失效（或存在未知门控），**APK 层面无解，需 BSP 层修**
- 测试音源方法沉淀：`am start -a android.intent.action.VIEW -d file:///sdcard/Music/test_tone.wav -t audio/wav`
- **下一步计划**：① app 内加"自播自采"测试（排除客户端 OpenSL/AudioTrack 因素）② 若仍失败→BSP 修 secondary outputs（与 FF 宏/自动 host/priv-app 预装攒一次 6h 编译）③ 此项不阻塞其他主线
